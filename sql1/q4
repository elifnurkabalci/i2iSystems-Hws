CREATE TABLE HR.EMP AS (SELECT * FROM HR.EMPLOYEES);

INSERT INTO HR.EMP  
(EMPLOYEE_ID, FIRST_NAME, LAST_NAME, EMAIL, PHONE_NUMBER, HIRE_DATE, JOB_ID, SALARY)  
VALUES  
(250, 'Erman', 'İbrisim', 'erman@ibrisim', 5556783245 , '3.10.2013', 'IT_PROG', 10000)

UPDATE HR.EMP  
SET PHONE_NUMBER=500000, SALARY=15000 
  
DELETE FROM HR.EMP  
WHERE EMPLOYEE_ID=250;

DROP TABLE HR.EMP;

--I have already errors